These files are source-only originals kept out of `src/assets` so the runtime asset tree only contains files the app actually imports.

- `open-science/`: original assets used to derive the optimized Open Science imagery.
- `case-studies/`: original case study source imagery kept for future exports.
- `earthcode/`: original Earthcode branding assets kept alongside the smaller shipped variants.
